# AGENTS.md

このリポジトリは、初学者がベクトルと微積分を使って高校物理を原理から理解するための、Quarto製教科書プロジェクトである。

## 作業前に読むもの

本文・図・問題を編集する前に、次を確認する。

1. `docs/editorial/style-guide.md`
2. `docs/editorial/review-checklist.md`
3. 対象章の `docs/chapters/*-design.md`
4. `docs/editorial/feedback-log.md` の関連項目

## 読者と文章

- 主読者は、高校物理を初めて学ぶ人。高校数学のベクトルや数学IIIの微積分は前提にしない。
- ユーザーから原稿・構成案が与えられている場合は、それを内容、順序、語り口の第一資料とする。独自の教育構成へ置き換える場合は、先に提案して合意を得る。
- 本文は簡潔な学術説明文とし、原則として「である」調で統一する。
- 用語の定義、数式、物理的意味を順に示し、簡潔かつ体系的に記述する。
- 箇条書き、表、calloutを連続させない。本文の流れを主とし、構造化要素は理解を助ける場合だけ使う。
- 会話的な呼びかけ、物語的な導入、過剰な比喩を基本の語り口にしない。
- 新しい式の直前に対象と計算内容を示し、式の直後に各項と結果の物理的意味を記す。
- 数学的操作、その図形的意味、物理法則、具体例を区別する。親しみやすさのために物理的条件や因果関係を省略しない。
- 基本概念の説明では `項目名 → 計算 → 物理的意味 → 簡単な例・問題` の簡潔な反復構造を優先する。編集意図や学習設計を説明するメタ文章は本文へ持ち込まない。

## Quarto・数式

- インライン数式は `$ ... $`、独立した表示数式は `$$ ... $$` を使う。
- `\[ ... \]` はMarkdownで壊れるため使わない。
- 単位は原則として数式内で `\mathrm{N}`、`\mathrm{m}` などとする。
- 数式は、HTML出力に `<span class="math">` またはMathJax用要素として残っていることを確認する。
- 図はSVGを基本とし、本文と `fig-alt` の双方で意味が伝わるようにする。色だけで情報を区別しない。

## フィードバックの蓄積

ユーザーから指摘を受けたときは、次の順で処理する。

1. 該当箇所を修正する。
2. 同種の問題をリポジトリ全体から検索する。
3. 再利用できる学びを `docs/editorial/feedback-log.md` に追記する。
4. 一般化できる原則なら `style-guide.md` または `review-checklist.md` に反映する。
5. ビルドと表示を確認する。

個別の好みを無理に一般則へしない。どの章にも効く再発防止策だけを規則へ昇格させる。

## 検証

変更後は少なくとも次を行う。

```bash
quarto render --to html
rg -n '^\\\[|^\\\]$' --glob '*.qmd'
git diff --check
```

さらに、変更したページについて数式、図、見出し番号、折りたたみ解答、前後リンクを確認する。生成先 `_book/` はコミットしない。

## ドキュメント構成

- `docs/book-plan.md`：全体構成と教育方針
- `docs/chapters/`：章ごとの設計書
- `docs/editorial/style-guide.md`：文章・説明・数式・問題の共通原則
- `docs/editorial/review-checklist.md`：公開前の確認項目
- `docs/editorial/feedback-log.md`：指摘から得た学びと反映先
- `docs/reference/`：本文へ再構成する前の素材・考察
