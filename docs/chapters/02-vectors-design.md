# 第2章「ベクトル」設計書

## 章の役割

本書全体で使う「向きのある量の読み書き」を身につける章とする。数学のベクトルを先に完成させてから物理へ進むのではなく、**操作 → 現象 → 座標による測定 → 新しい操作**を往復する。

この章は本書の試作章でもある。説明、図、短い問題、「微積で見る」欄の基本レイアウトをここで検証し、後続章のテンプレートにする。

## 前提知識

- 正負の数と四則演算
- 比例
- 直角三角形と三平方の定理
- 三角比は章内で必要なところを復習する

高校数学でのベクトル既習を前提にしない。

## 章末の到達目標

読者が次のことをできる状態を目指す。

1. スカラーとベクトルを、単なる「数字と矢印」ではなく物理量の性質として区別できる。
2. ベクトルの加減算とスカラー倍を、図と成分の両方で計算できる。
3. 位置、変位、速度、加速度、力がどのようなベクトルか説明できる。
4. 一つのベクトルを選んだ座標軸の成分として表し、再び合成できる。
5. 同じベクトルでも座標軸を変えると成分が変わることを説明できる。
6. 内積を「一方の向きへの寄与」、外積を「回転させる効果」として説明できる。
7. 問題に応じて、図・大きさと向き・成分表示のどれを使うか選べる。

## 章を貫く問い

> 向きのある量を、どうすれば計算できる形で測り、組み合わせられるだろうか。

## 節構成

### 2.0 導入：二人で荷物を引く

二人が異なる向きにロープを引く場面から始める。「$30\,\mathrm{N}+40\,\mathrm{N}=70\,\mathrm{N}$ としてよいか」を予想させ、大きさだけでは結果が決まらないことを示す。

- 観察：引く向きが同じ、反対、直角の場合
- 最初の問い：向きの違いを計算へどう入れるか
- この時点では成分計算を教えず、必要性を作る

### 2.1 大きさだけの量、向きも必要な量

- スカラー：質量、時間、温度、エネルギー
- ベクトル：変位、速度、加速度、力
- 「正負」と「向き」は同じではなく、正方向を決めて初めて符号が意味をもつ
- 単位を含めて物理量を表す

**手を動かす**：量をスカラーとベクトルに分類し、判断理由を一言で書く。

### 2.2 ベクトルを矢印で表す

- 矢印の長さと向き
- 始点を移しても大きさと向きが同じなら同じ自由ベクトルとして扱う
- ベクトルの大きさ $|\boldsymbol{A}|$
- 等しいベクトル、逆ベクトル、零ベクトル

**誤解への対応**：「矢印の置かれた場所」そのものが常にベクトルの一部とは限らない。位置ベクトルとの違いは第3章で深める。

### 2.3 足す・引く：変化をつなぎ、効果を重ねる

- 三角形則と平行四辺形則
- 結合法則と交換法則
- 引き算を逆ベクトルの足し算として捉える
- 変位をつなぐ例と、複数の力を重ねる例

**手を動かす**：方眼上の経路、同一直線上の力、直角方向の力を順に扱う。

### 2.4 何倍かする：スカラー倍と単位ベクトル

- 正の倍率、負の倍率、ゼロ倍
- 大きさ1の単位ベクトル
- $\boldsymbol{F}=m\boldsymbol{a}$ を「ベクトルのスカラー倍」として予告する

**手を動かす**：図示、倍率の決定、向きの反転を短問で確認する。

### 2.5 物理現象をベクトルで整理する

- 位置と変位
- 平均速度と加速度は「変化量」から生まれるベクトル
- 合力と重ね合わせ
- 現象を「対象を選ぶ → ベクトルを描く → 組み合わせる」の順で整理する

ここでは運動公式や運動方程式を解かず、第3章・第6章への橋を架ける。

### 2.6 座標でベクトルを測る

- 原点、軸、正方向を選ぶ
- 直交する単位ベクトル $\boldsymbol{i},\boldsymbol{j}$
- $\boldsymbol{A}=A_x\boldsymbol{i}+A_y\boldsymbol{j}$
- 図から成分を読み、成分から矢印を復元する
- $|\boldsymbol{A}|=\sqrt{A_x^2+A_y^2}$
- 三角比による $A_x=A\cos\theta$, $A_y=A\sin\theta$

**重要な主張**：成分はベクトルそのものではなく、選んだ軸に沿って測った値である。

### 2.7 成分で計算する

- 加減算とスカラー倍を成分ごとに行う
- 基底ベクトルが線形独立であることを、矢印と係数比較から直感的に説明する
- ベクトル方程式を成分の式へ分けられることを示す

**注意**：成分ごとに式を書けることと、現象の各方向が常に物理的に独立であることを混同しない。

### 2.8 内積：ある向きへの寄与を取り出す

- 射影としての $\boldsymbol{A}\cdot\boldsymbol{B}=AB\cos\theta$
- 成分表示 $A_xB_x+A_yB_y$
- 結果がスカラーになる意味
- 垂直なら内積が0になる
- 仕事 $W=\boldsymbol{F}\cdot\boldsymbol{x}$ を第8章の予告として扱う

**手を動かす**：同方向、逆方向、直角、斜めの順で符号と大きさを予想してから計算する。

### 2.9 外積：回転させる効果を表す

- 大きさ $|\boldsymbol{A}\times\boldsymbol{B}|=AB\sin\theta$
- 向きは二つのベクトルに垂直で、右ねじの規則で決める
- 順序を逆にすると向きが反転する
- 力のモーメント $\boldsymbol{\tau}=\boldsymbol{r}\times\boldsymbol{F}$
- ドアを押す位置と向きを具体例にする

初回は意味と図を優先する。3次元の行列式による成分計算は本文の必須事項にせず、発展欄へ置く。

### 2.10 まとめ：表し方を選ぶ

同じ問題を、矢印、成分、内積・外積のいずれで扱うと見通しがよいか比較する。

- つなぐ・重ねる：加減算
- 伸ばす・反転する：スカラー倍
- ある向きへの寄与：内積
- 回転させる寄与：外積
- 数値として計算する：成分表示

## 問題配置

### 節内の短問

- 新しい概念一つにつき1〜3問
- 30秒〜3分で解ける量にする
- 「計算する」前に「向き・符号・大小を予想する」問いを置く
- 答えだけでなく、判断理由を一文で書く問題を含める

### 章末問題

- 基礎：用語、図示、単純な成分計算（6問程度）
- 標準：複数操作を組み合わせる物理場面（4問程度）
- 探究：軸の選び方を比較する問題、説明問題（2問程度）
- 合計12問程度を初期目安とする

## 必要な図

1. 二人がロープで荷物を引く導入図
2. 同じ向き・反対向き・直角のベクトル加法
3. 三角形則と平行四辺形則
4. ベクトルの成分分解と再合成
5. 軸を回したとき、矢印は同じで成分だけが変わる比較図
6. 射影としての内積
7. ドアと力のモーメント
8. 右ねじの規則を示す外積の立体図

図は色だけに意味を持たせず、線種・矢印・ラベルでも区別できるようにする。

## この章で扱わないこと

- 基準系、慣性系、非慣性系の本格的な議論（第3章以降）
- 運動方程式を用いた力学問題（第6章）
- 仕事とエネルギーの体系的な議論（第8章）
- 剛体のつり合いとモーメントの符号規約（第7章）
- 3次元外積の行列式計算（発展）
- 抽象的なベクトル空間の公理（大学数学への発展）

## 原稿化の順序

1. 2.0〜2.4で、図と短問を含む基本操作を完成させる。
2. 2.5〜2.7で、物理量と座標成分への橋を作る。
3. 2.8〜2.9で、内積・外積を具体的な現象へ結びつける。
4. 章末問題とまとめを作る。
5. 初学者一名以上に試読してもらい、未定義語と飛躍を修正する。

## 完了条件

- すべての到達目標が少なくとも一つの問題で評価されている。
- 新しい専門語は使用前または使用直後に定義されている。
- 各節に、図・予想・短問のうち少なくとも二つがある。
- HTML版で数式、図、脚注、章内リンクが正しく表示される。
- 読者が第3章へ進むために必要なベクトル表記を自力で読める。
