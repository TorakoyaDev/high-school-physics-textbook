# 第1章「ベクトル：大きさと向きを表す方法」設計記録

## 正本

本文の正本は `docs/reference/ch01-final-source.md` である。「壁打ち（ベクトルの基本）」タスクにおける2026年9月12日時点の最後の全体稿を、未編集で保存したものである。

公開用 `mechanics/vectors.qmd` では、次の変換だけを行う。

1. 原稿先頭の章見出しをQuartoのYAMLタイトルへ移す。
2. 自動採番を無効にし、確定稿の節番号をそのまま表示する。
3. 数式区切り `\(...\)` / `\[...\]` を `$...$` / `$$...$$` に変換する。

上記以外の文章、見出し、順序、数式内容は変更しない。

## 確定した章構成

1. 数だけでは表せない情報
2. ベクトルを矢印で表す
3. ベクトルの等しさと矢印の置き場所
4. ベクトルを成分で表す
5. ベクトルの合成と加法
6. ベクトルのスカラー倍
7. ベクトルの内積
8. ベクトルの外積
9. まとめ

## 次章への接続

第1章では、分解と再合成の体系的な説明を行わない。第2章で、1本のベクトルを方向ごとのベクトルへ分解し、係数比較によってベクトル方程式を成分ごとのスカラー方程式として読む方法を扱う。

## 検証

```bash
python3 scripts/verify_approved_sources.py
quarto render --to html
```

正本のSHA-256は次のとおりである。

```text
e5ad5c6004742c54bf22a029f3866f3d978340602b8d9d6dfe9d56b32dbfd136
```
